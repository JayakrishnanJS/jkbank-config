build:
  version: "1.0"

# --- Business Data ---
accounts:
  message: "Welcome to the Accounts Service APIs production support team of JK Bank"
  contactDetails:
    name: "Steve - Product Manager"
    email: "{cipher}3b34730332c9fc6d1bf0dc52e3e2d6f674f514895820cf973541a4e9b9e6ef9c"
  onCallSupport:
    - 1800-123-456
    - 1800-987-000

# --- Kafka & Messaging Configuration ---
spring:
  application:
    name: accounts
  cloud:
    function:
      definition: updateCommunication
    stream:
      default-binder: kafka
      bindings:
        # Input: Receive feedback from Message Service
        updateCommunication-in-0:
          destination: feedback-account-communication-sent
          group: accounts
        
        # Output: Send SMS/Email request to Message Service
        sendCommunication-out-0:
          destination: send-account-communication
      kafka:
        binder:
          brokers:
            - kafka:9092
